#include <iostream>#include <array>using namespace std;template <int N> array<int, N> sum(array<int, N> arr1, array<int, N> arr2) {  int temp = 0;  auto p_arr1 = begin(arr1);  auto p_arr2 = begin(arr2);}int main(int argc, char *argv[]) { return 0; }